[ ] parsing single quotes inside double quotes 
	putglobal.c :44 and :86 add flag if we are entered double quotes
	and do not skip single while its active

[ ] add correct sig hander while here_doc input (<<limiter) 
	message expected EOF if Ctrl-D
	open_in_outs.c :60

[ ] fix redirect parsing and waitchildren
	pipexv2/

[ ] fix built in 
	cd.c
	export.c (export (NONE))

[ ] add builtin to pipex and check nofork

[ ] add errors messages in return in command and redirect parsings 

[ ] errro message in check redir in
	put_redir.c :17 :37
